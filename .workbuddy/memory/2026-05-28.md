# 2026-05-28 工作记录

## 自动化任务升级：AI 早报 → 自动写入仓库

**任务**：将每日 AI 早报自动化从"生成文件并交付"升级为"生成文件 + 写入仓库 + git push"

**改动内容**：
- 修改自动化 `automation-1779852562690`（前端下一步 · AI 早报 | by 安东尼）
- 原有 Step 4（配文）→ Step 6，Step 5（交付）→ Step 7，Step 6（记录）→ Step 8
- 新增 **Step 4A**：替换 `web/components/LiveSignalBoard.tsx` 中的 `signals[]` 数组（首页卡片流）
- 新增 **Step 4B**：写入 `web/content/briefs/ai-daily-YYYYMMDD.md`（/brief 独立内容页）
- 新增 **Step 5**：git add → commit → push，SSH 优先（本机 SSH 已通），失败时改 remote 为 SSH URL 再 push

**关键路径**：
- 首页组件：`web/components/LiveSignalBoard.tsx`，`signals` 数组在第 15 行
- brief 目录：`web/content/briefs/`，frontmatter 字段：title/slug/date/category/tldr/readMinutes/pro/tags
- 仓库 remote：`https://github.com/TUARAN/frontend-weekly-digest-cn.git`（SSH 已配置，可直接 push）
- SSH 验证：`Hi TUARAN! You've successfully authenticated` ✅

**git commit 格式**：`chore: AI 早报 YYYY-MM-DD - 今日精选 X 条动态`

## 手动测试执行（2026-05-28 10:15）
- 完整跑通 8 Steps 全流程
- 推送 commit e080ae3，包含 LiveSignalBoard.tsx + ai-daily-20260528.md
- SSH 通过 HTTPS remote 正常推送（git 自动识别 SSH key）
- Vercel 已触发构建

## AI 早报嵌入首页
- 将 `/Users/tuaran/WorkBuddy/Claw/ai-daily-0528.html` 复制到 `web/public/`
- 在 `AiRadarHome.tsx` 首页顶部新增「AI 早报」板块，使用 500×820 iframe 嵌入
- 本地服务运行在 3333 端口（nohup，PID 8035）

## AI 早报自动化更新机制
- AiRadarHome.tsx 的 iframe 从 `ai-daily-0528.html` 改为 `ai-daily-latest.html`
- 自动化 Step 3 新增：每次生成完 MMDD.html 后自动 cp 覆盖 `web/public/ai-daily-latest.html`
- 自动化 Step 5 的 git add 已包含 `web/public/ai-daily-latest.html`，每天 push 后 Vercel 自动部署更新
- 已推送 commit 4827290

## AI 早报 manifest 索引改造（2026-05-28 10:44）

**问题**：之前每次自动化覆盖 `ai-daily-latest.html`，历史早报丢失，无法切换查看。

**改造方案**：
1. **新增 `web/public/ai-daily-manifest.json`** — 所有早报的元数据索引
   - `list[]`：每期 date / displayDate / file / count / highlights[]
   - `latest`：最新一期日期
   - 历史文件 (`ai-daily-MMDD.html`) 永久保留，不会覆盖

2. **新增 `web/components/AiDailyBoard.tsx`** — 客户端组件
   - `useEffect` fetch manifest → 默认展示 latest（index 0）
   - 左右箭头 `<ChevronLeft>` / `<ChevronRight>` 切换历史日期
   - 「最新」绿色呼吸灯标签
   - 高亮摘要 pills 展示每期要点
   - 空状态 / 加载态 / 错误态全部覆盖

3. **改造 `AiRadarHome.tsx`** — 整段替换为 `<AiDailyBoard />`

4. **更新自动化** `automation-1779852562690`：
   - Step 3 从"覆盖 latest.html" 改为"追加 manifest 条目"（3B copy 到 public + 3C 更新 manifest JSON）
   - Step 5 git add 从此改为 4 个文件：`MMDD.html + manifest.json + LiveSignalBoard.tsx + brief.md`
   - 不再提交 `ai-daily-latest.html`

**推送 commit**：3bb9126

## AI 雷达首页水平翻页重构（2026-05-28 10:57）

**改动背景**：用户要求 ① iframe 非滚动全显示 + 缩小、② 添加 syncblog.cn 链接、③ 从纵向滚动改为横向翻页。

**改动内容**：

1. **AiDailyBoard.tsx** — iframe 改为 `transform: scale(0.68)` + `scrolling="no"`
   - 容器 `340×557px`，iframe 源尺寸 `500×820px` 缩放后全显示
   - 无滚动条，卡面完整可见

2. **新增 `PageCarousel.tsx`** — 通用水平翻页组件
   - Props: `pages[]`（key/label/icon/content）+ `headerExtra?`
   - 顶部 Tab 栏 + 左右箭头（桌面端悬停）+ 底部圆点指示器 + 页码
   - 支持键盘 ← → 导航 + 触摸滑动
   - 进出动画：`translate + opacity` 200ms

3. **重构 `AiRadarHome.tsx`** — 四页结构
   - Hero 区保留在顶部（标题 + 三档内容概览 + syncblog.cn 入口）
   - Page 1: **AI 早报**（AiDailyBoard）
   - Page 2: **短文**（速递样本 + 信号源优先级）
   - Page 3: **中文**（精译选题 + 翻译流水线 + Prompt 资产）
   - Page 4: **长文**（差异化策略 + 集成路线 + CTA）
   - syncblog.cn 链接：Hero 区按钮 + 翻页 Tab 栏右侧 badge

**未推送**，待用户确认效果后 push。

## Hero 区精简 + 删除 mock 数据（2026-05-28 11:01）
- 去掉大块 Hero（标题+长文案+三档卡片），改为一行紧凑标题
- 删除所有 mock 数据：briefItems、digestItems、信号源、流水线、Prompt、差异化策略、集成路线
- 页面 2-4 只保留标题+描述+跳转按钮
- 全局「决策简报」→「简报」（Header/Footer/brief/Pro/Order 共 6 个文件）

## 删除首页按钮 + 删除简报模块（2026-05-28 11:11）
- 删除首页"浏览全部"按钮和"博主联盟"badge
- 彻底删除简报模块：
  - 删除 `app/brief/`、`lib/briefs.ts`、`components/BriefCard.tsx`、`content/briefs/*.md`
  - 清理 Header/Footer/AiRadarHome/Pro/Roadmap/Order/layout/NewsletterSignup/order-plans 中的简报引用
  - AiRadarHome 页面 2-4 的"浏览"按钮全部删除


## 导出图片 + 分享页功能
- 安装 `html-to-image` 依赖
- `AiDailyBoard.tsx` 添加「分享」和「导出图片」按钮于右上角
  - 导出：html-to-image `toPng` 捕获 iframe `.card` → 2x PNG 下载
  - 分享：复制 `/share/ai-daily?date=YYYY-MM-DD` 到剪贴板
- 创建分享页 `app/share/ai-daily/page.tsx`（暗色背景独立页）
  - 顶部：返回首页 + 复制链接 + 下载图片
  - 主内容：iframe 100% 全宽展示 + 摘要 + 分享引导
- iframe 缩放调整：scale(0.71) → 容器 355×620

## 修复 iframe 内容截断（2026-05-28 11:26）
- 4 条新闻 + footer 实际高度约 976px，910px iframe × 0.71 = 646px，容器 620px 不够
- 调整 iframe 高度 1150px、容器高度 820px（1150×0.71≈816，足够展示 5 条新闻）

## 修复 Mac 触控板滑动问题（2026-05-28 11:34）
- **问题**：Mac 触控板水平双指滑动触发浏览器后退/前进（跳转 404）；触屏滑动切换了顶部 Tab
- **PageCarousel.tsx 改动**：
  - 删除 `touchStart` ref + `onTouchStart/onTouchEnd` 处理 + 键盘 Arrow 导航 `useEffect`
  - 删除内容容器的 `onTouchStart`/`onTouchEnd` 绑定
  - 内容容器添加 `overscrollBehaviorX: 'none'` 阻止浏览器导航
- **AiDailyBoard.tsx 改动**：
  - 新增 `touchStartX` ref + `lastWheelTime` ref
  - 新增 `handleTouchStart`/`handleTouchEnd`：水平滑动 > 50px 切换日期，`stopPropagation()` 防冒泡
  - 新增 `handleWheel`：Mac 触控板双指水平滑动切换日期，200ms 防抖，阈值 deltaX > 30，`preventDefault + stopPropagation`
  - 主内容容器绑定 `onTouchStart`/`onTouchEnd`/`onWheel`

## 新增周刊翻译自动化
- **自动化 ID**：`automation-1779951288854`
- **名称**：前端周刊 · 周刊翻译 | by 安东尼
- **时间**：每周一 07:00 执行
- **信息源**：Medium @frontender-ua（Fresh Frontend Links），Frontend Weekly Digest 系列
- **工作流**：搜索最新期 → 抓取内容（curl/agent-browser/WebFetch 三级兜底）→ 检查幂等 → 翻译主文件 → 翻译精选 3-5 篇 → 更新 README → git push → 写记录
- **注意**：Medium 直接 WebFetch 会失败，需要 curl 带 UA 或 agent-browser
- **最佳抓取方式**：Medium RSS feed `https://medium.com/feed/@frontender-ua` 可以直接获取完整文章列表（含分类和链接），WebFetch 可以成功获取
